%h1.page-header Stichwort anlegen
%row
  = form_for @changeset, keyword_path(@conn, :create), fn f ->
    =render("_keyword.html", f: f, categories: @categories)
    .form-group.row
      .offset-sm-2.col-sm-10
        =submit("Neues Stiwchwort anlegen", class: "btn btn-primary")
  - end 
