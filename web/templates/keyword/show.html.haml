%h1.page-header Stichwort bearbeiten
%row
  = form_for @changeset, keyword_path(@conn, :update, @id), fn f ->
    =render("_keyword.html", f: f, categories: @categories, changeset: @changeset)
    .form-group.row
      .offset-sm-2.col-sm-10
        =submit("Änderungen speichern", class: "btn btn-primary")
  - end 
