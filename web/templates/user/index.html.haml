.row.page-header
  .col-xs-12
    %h1 Benutzer
      .float-xs-right
        =link("Neuer Benutzer", to: user_path(@conn, :new), class: "btn btn-outline-primary")
%table.table.table-hover
  %thead
    %tr
      %th Name
      %th Login
      %th Aktiv
      %th Admin
  %tbody
    = for user <- @users do
      %tr
        %td= link(User.name(user), to: user_path(@conn, :show, user.id))
        %td= user.username
        %td
          - if user.active do
            %i.fa.fa-check
          - else
            %i.fa.fa-ban
        %td
          - if user.admin do
            %i.fa.fa-check
          - else
            %i.fa.fa-ban

    - end

